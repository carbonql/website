---

version: "3"
services:
  website:
    container_name: website
    build:
      context: .
    volumes:
      - .:/code
      - node_modules:/code/node_modules
    ports:
      - 3000:3000

volumes:
  node_modules:
